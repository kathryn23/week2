
function count(arra1,arra2) {
  if not isinstance(txt, str):
  var vowels = {};
  var Array = [];

  arra1.forEach(function (vowels) {
    if(!object[vowels]) object[vowels] = 0; object[vowels] += 1;
  })
  for (var i in vowels) {
     if(vowels[i] >= 2) {
        vstring += vowels(i)

     }
  }
console.log(Array.prototype.slice.call(ajambokathryn));
}
 vowel_count(txt):
  if not isinstance(txt, str):
      raise TypeError('invalid input')

  vowels = 'aeiou'
  newtxt = txt.lower()
  noduplicates = set(newtxt)
  duplication = len(newtxt)-len(noduplicates)
  vowelstring = ""

  for i in vowels:
      if i in noduplicates:
          vowelstring += str(i)

  return (vowelstring, duplication)
